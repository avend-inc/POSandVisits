# NOTIME 日次KPI自動化 — 要件・仕様書 v2

> **v1からの変更点**
> v1はDB導入前・実サイト調査前に書かれたもので、事実と異なる箇所がありました。
> 2026-07-22 に実際のサイトを調査して確定した内容に差し替えています。
> v1で「Playwright前提」としていた部分は、**不要になりました**（§3参照）。
> 元の文書は `IMPLEMENTATION_v1_original.md` に残してあります。

---

## 1. このツールは何をするのか

古着屋チェーン「NOTIME」の日次KPIを、毎朝自動で集計する。

```
毎朝8時（JST）
  ↓
① 各サービスからCSVを取得
  ↓
② adapters.adapt() で共通の形に変換
  ↓
③ DB（notime.db）に「明細のまま」保存   ← ここが心臓部
  ↓
④ DBから読み出して集計
  ↓
⑤ スプレッドシートに書き込み／Slackに通知（月・木）
```

**絶対のルール: 取得したCSVを直接集計しない。必ず一度DBに入れて、DBを正とする。**

理由は、集計はいつでも作り直せるが、捨てた明細は二度と戻らないから。
「5月の土曜の15〜18時にアウターは何着売れたか」のような後出しの問いに答えられるのは、
明細をそのまま貯めているからで、集計結果だけ保存していたら不可能になる。

---

## 2. ファイル一覧と状態

| ファイル | 役割 | 状態 |
|---|---|---|
| `config.py` | 店舗・POS構成の設定 | ✅ 完成・**変更禁止** |
| `adapters.py` | POSごとのCSV形式を共通スキーマに変換 | ✅ 完成・**変更禁止** |
| `transform.py` | 按分・KPI算出・課題診断 | ✅ 完成・**変更禁止** |
| `db.py` | SQLite保存層 | ✅ 完成・**変更禁止** |
| `fetch_digitail.py` | 入店データ取得（山形・いわき・福井） | ✅ 実装済み・**未実行** |
| `fetch_cashier.py` | POSデータ取得（山形・いわき・福井） | ⬜ 未着手 |
| `fetch_airregi.py` | POSデータ取得（下北沢） | ⬜ 未着手 |
| `fetch_ezregi.py` | POSデータ取得（下北沢） | ⬜ 未着手 |
| `push_sheets.py` | スプレッドシート書き込み | ⬜ 未着手 |
| `push_slack.py` | Slack通知（月・木） | ⬜ 未着手 |
| `main.py` | 全体の司令塔 | ⬜ 未着手 |

「変更禁止」の4ファイルは実データで検証済み。**触らない。**
どうしても足りないものがあれば、新しいファイル側で吸収する。

---

## 3. データソース

### 3-1. デジテールストア（入店データ）— ✅ 実サイトで確定済み

| 項目 | 内容 |
|---|---|
| URL | `https://dashboard.digitail-tech.com` |
| ログイン | `POST /auth/login` — 項目は `username` と `password` の**2つだけ** |
| 隠しトークン | **無し** → ブラウザを使わずログインできる |
| CSV取得 | `GET /{スラッグ}/kpi/visits/download?interval=day&from=YYYY-MM-DD&to=YYYY-MM-DD` |
| 文字コード | UTF-8（BOM付き）→ `utf-8-sig` |
| 対象店舗 | 山形・いわき・福井 |

**⚠️ v1の誤りを訂正**

| v1の記述 | 実際 |
|---|---|
| `input[name="email"]` | **`input[name="username"]`**（メールアドレスではなくユーザー名） |
| 「Playwrightでダウンロードボタンを押す」 | **ボタンの正体はただのリンク。押す必要なし** |

**店舗スラッグ対応表**（`config.py` が変更禁止なので `fetch_digitail.py` が持つ）

| config.pyの店舗名 | URLスラッグ |
|---|---|
| 山形 | `notime_yamagata` |
| いわき | `notime_iwaki` |
| 福井 | `notime_fukui` |
| （下北沢） | `notime_shimokitazawa` ※対象外・§5-2参照 |

**CSVの中身**（`transform.normalize_entry()` は左3列だけを使う）

```
日付,無人入店/解錠数,無人常連来店数,無人購買回数,無人購買率 (%)
2026-03-20,612,91,0,0
```

| CSVの列 | → 共通スキーマ |
|---|---|
| 日付 | `date` |
| 無人入店/解錠数 | `来店客数` |
| 無人常連来店数 | `常連来店数` |

**実測**: 開店日〜前日を1リクエストで取得できることを確認済み
（山形124日／いわき88日／福井39日、いずれも欠けなし）。

### 3-2. cashier（POSデータ）— ⬜ 未調査

| 項目 | 内容 |
|---|---|
| URL | `https://cashier.jp` |
| 認証 | ID/PW |
| 対象店舗 | 山形・いわき・福井 |
| CSV形式 | ✅ `adapters.adapt_cashier()` が実データで検証済み |

CSVの**形式は確定済み**。未調査なのは「サイトからどう取ってくるか」だけ。

### 3-3. Airレジ / EZレジ（POSデータ・下北沢）— ⬜ 未調査

CSV形式は `adapters.py` に実データ確認済みのコメントがあり、確定している。

- Airレジ: Shift-JIS(cp932) / 1行=1明細 / 小計=税抜・合計=税込 / バンドル未使用
- EZレジ: UTF-8(BOM付き) / 1行=1取引・**商品明細なし** / 合計金額は**税込** / 「支払」のみ対象

**⚠️ EZレジは商品明細が無い**ため、下北沢のカテゴリ別・価格帯別分析は
EZレジ分について不可能。売上・購入客数・客単価・商品単価は正しく出せる。

---

## 4. 取得のルール

### 4-1. 当日のデータは必ず捨てる

エクスポート当日の数字は未確定。実測データ:

| 日付 | 取得時点 | 福井の伝票数 |
|---|---|---|
| 6/20 | 6/20 17:05 | 72件（未確定） |
| 6/20 | 6/22 09:34 | **113件（確定）** |
| 6/22 | 6/22 09:34 | 5件（未確定） |
| 6/22 | 6/24 15:41 | **46件（確定）** |

→ **取得する最終日 = 実行日の前日**。

**⚠️ 時差に注意**: GitHub Actions は UTC で動く。
「前日」は必ず日本時間（UTC+9）で計算すること。しないと1日ズレる。

### 4-2. 毎回まるごと取り直す

過去日の数字も後から増える。「一度取り込んだ日は触らない」は**NG**。

- 入店データ: 開店日〜前日を毎回まるごと取得
- DB側は `db.import_entries()` が日付キーで上書きするので、何度実行しても正しい状態になる
- POSデータは `db.import_pos()` が (店舗, レジ, 日付) 単位で削除→挿入するので同様

### 4-3. 生CSVは加工前に必ず保存

```
raw/
  digitail/2026-07-22_山形.csv
  cashier/2026-07-22_TradeDetail.csv
  airregi/2026-07-22_下北沢.csv
  ezregi/2026-07-22_下北沢.csv
```

ファイル名の日付は**取得した日**。スクレイパーが壊れて数日気づかなくても、
生データがあれば後から復元できる。

### 4-4. 失敗は黙って死なない

以下は必ず分かるメッセージを出す（将来的にはSlackへも）:

- ログイン失敗／セレクタが見つからない
- CSVが空、想定の列が無い
- 取得データに前日が含まれていない
- 行数が前回比で急減（通常の30%未満）

### 4-5. リトライ

3回まで、待ち時間を倍にしながら再試行（2秒 → 4秒 → 8秒）。

### 4-6. 書き込む前に自己検証

検証を通らなければ**書き込まずにエラー**。壊れたデータで上書きするのが最悪の事故。

---

## 5. 【重要】v1との矛盾点と、その決定

調査の結果、v1の記述と実態が食い違う点が見つかった。以下のとおり決定する。

### 5-1. `raw/` をGitにコミットするか → **しない**

| | 言っていること |
|---|---|
| v1 §9 | GitHub Actions で `git add raw/` して毎日コミット |
| ユーザー指示 | `.gitignore` に `.env` と `raw/` を追加 |

`.gitignore` に入れたものは `git add` できないので、両立しない。
**ユーザー指示を優先し、`raw/` はコミットしない。**

**結果として何が起きるか（把握しておくこと）**

- 手元のPCで実行 → 生CSVは自分のPCに残る ✅
- GitHub Actionsで実行 → 実行環境は毎回捨てられるので、**生CSVは残らない** ⚠️

つまり§4-3の「復元用アーカイブ」は、Actions運用では機能しない。
その代わり **`notime.db` をコミットする**ことで、日次データそのものは保全される。
DBには明細粒度で入っているので、実用上の復元力はDBが担う。

→ `main.py` を作るときに、この方針で問題ないか再確認する。

### 5-2. 下北沢の入店データ → **取得しない**

v1と `config.py` は「下北沢は入店カウンターが無く来店客数が取れない」としているが、
**実際にはデジテールにデータが存在した**（`notime_shimokitazawa`、7/1=10人、7/2=12人）。

ただし `config.py` は変更禁止ファイルで、`has_entry_data=False` になっている。
**現状維持とし、取得しない。**

`fetch_digitail.py` は `config.stores_with_entry_data()` を見て対象を決めるので、
将来 `config.py` の設定を `True` に変えれば、コードを直さなくても対象に入る。
（スラッグ表には下北沢も登録済み）

→ ただし数字が実態と合っているかは未確認。**判断はユーザーに委ねる。**

### 5-3. デジテールに13店舗あった → **3店舗のみ対象**

デジテールには鎌ヶ谷・下北沢・小倉魚町・早稲田・熊谷・静岡・伊予松前・浜松・
山形・いわき・天王台・福井・高松の13店舗が登録されている。
`config.py` は4店舗のみ。**指示どおり山形・いわき・福井の3店舗だけを対象とする。**

店舗を増やすときは `config.py` に1エントリ足し、スラッグ表に1行足すだけでよい。

### 5-4. ログイン方式 → **requests優先、Playwrightは保険**

ログイン画面に隠しトークンが無く、CSVもただのリンクだったため、
ブラウザを起動せずに完結できる。まず `requests` で試し、
失敗したときだけ Playwright に自動で切り替える。

利点: 速い／GitHub Actionsでブラウザのインストールが不要／UI変更に強い。
保険を残す理由: 将来サイトがJavaScriptログインに変わっても止まらないようにするため。

### 5-5. 入店データは「無人時間のみ」である

デジテールの入店データ画面には「無人時間の入店データを表示します」と明記されている。
つまり**有人営業時間中の来店は数に入っていない可能性がある。**

購買率＝購入客数÷来店客数 なので、もし有人時間の来店が抜けているなら、
**購買率は実態より高く出る。**

v1 §11 の検証用の数字（いわき24.6%／福井29.4%）も同じデータから出ているため、
既存の数字とは整合する。**現状の定義のまま進める**が、
「購買率は無人時間ベース」という前提は認識しておく必要がある。

→ 実店舗の運用（無人営業が何時間あるか）を知っているユーザーの確認が必要。

---

## 6. 集計ロジックの落とし穴（`transform.py` に実装済み・変更不要）

1. **日付形式の揺れ**: cashierは `2026-03-20` と `2026/6/19` の両方を返す（吸収済み）
2. **店舗名の表記ゆれ**: `NOTIMEいわき（2026/4/25~2026/4/28)` のような括弧付きが混じる（正規化済み）
3. **バンドル（セット割）の按分**: POSはセット割の親行を「セール商品」に計上する。
   **売上の20%超**がここに埋もれるため、構成品の販売価格比で実カテゴリへ按分（実装済み）
4. **税抜で統一**: 売上・客単価・商品単価はすべて税抜。「純売上」＝税抜／「総売上」＝税込。混ぜない
5. **カテゴリの店舗差**:
   - Tシャツは新分類（無地/プリント/バンド/アニメ）のまま保持。**統合しない**
   - 福井のみ「Tシャツ」タグ＝ラグとして読み替え（`category_override`）
   - レジ袋・クーポン・不明は物販から除外

---

## 7. スプレッドシート出力（未実装）

**既存のスプシに新規シートとして追加**（新規スプシは作らない）。
`gspread` + サービスアカウント。**対象スプシをサービスアカウントのメールに「編集者」で共有**すること。

| シート名 | 内容 |
|---|---|
| `日次KPI_{店舗}` | date / 曜日 / 来店客数 / 購入客数 / 購買率 / 購入点数 / 売上税抜 / 売上税込 / 客単価 / 平均購入数 / 商品単価 / 来店単価 |
| `レジ別_{店舗}` | POS別の売上・購入客数・構成比（下北沢のような複数レジ店舗用） |
| `カテゴリ日次_{店舗}` | date × カテゴリ の販売数マトリクス |
| `価格帯_{店舗}` | 価格帯別 売上・販売数・構成比 |
| `診断ログ` | 実行日 / 店舗 / ボトルネック / 各KPI前週比 |

- シート名は `config.STORES` をループして自動生成（新店舗追加時に自動で増える）
- 入店データが無い店舗は、購買率・来店単価の列を空欄にする
- **追記ではなく全書き換え**。「何度実行しても同じ結果」が絶対条件
- 数字の出どころは**必ずDB**（`db.daily_kpi()` など）。CSVから直接作らない

---

## 8. Slack通知（未実装・月木のみ）

```python
if datetime.now(JST).weekday() in (0, 3):   # 月=0, 木=3
    push_slack()
```

```
📊 NOTIME KPI（{期間}）

【福井】日商 ¥XXX,XXX（前週比 +X%）
  来店 XXX/日 ・ 購買率 XX.X% ・ 客単価 ¥X,XXX ・ 商品単価 ¥X,XXX
  ⚠️ ボトルネック: 来店客数（前週比 -XX%）

【下北沢】日商 ¥XXX,XXX（前週比 +X%）  ※入店データ無し
  購入 XX人/日 ・ 客単価 ¥X,XXX ・ 商品単価 ¥X,XXX
  レジ内訳: Airレジ ¥XXX,XXX / EZレジ ¥XXX,XXX

💡 示唆
（Claude APIが生成）
```

### 示唆生成プロンプト（Claude API / claude-sonnet-5）

```
あなたはアパレル小売のデータアナリストです。
NOTIME（古着チェーン）のKPIを分析し、課題を特定してください。

【前提】
- 売上 = 来店客数 × 購買率 × 平均購入数 × 商品単価
- 日次売上との相関は「来店客数」が圧倒的（Spearman 0.87〜0.92）。
  購買率・客単価・商品単価の相関は低い（0.1〜0.4）。
  → 売上の振れは基本的に「集客」で決まる。
- 営業利益率50%が目標。安易な値引きは粗利を壊すので推奨しない。
- 価格帯は ¥2,980〜3,980 が数量の主戦場、¥5,980以上が単価源。
- 一部店舗（下北沢）は入店データが無く、購買率・来店単価は算出不能。
  その店舗は購入客数ベースで評価すること。

【データ】
{各店のKPI・前週比・寄与度・ボトルネック}
{カテゴリ別 売上/販売数 構成比}
{価格帯別 売上/販売数 構成比}
{複数レジ店舗はレジ別内訳}

【出力】
1. 各店の最大のボトルネックはどのKPIか
2. カテゴリ・価格帯の観点で、売れ筋の変化や在庫の偏りはないか
3. 具体的な打ち手（値引き以外を優先）
簡潔に、経営会議で使える粒度で。
```

---

## 9. 診断の観点

```
売上 = 来店客数 × 購買率 × 平均購入数 × 商品単価
                └─ 購入客数 ─┘   └──── 客単価 ────┘
```
（入店データ無しの店舗は「購入客数 × 平均購入数 × 商品単価」で分解）

- **来店客数**: 最重要。売上との相関0.9。落ちていれば集客施策が答え
- **購買率**: 来店の質。落ちていれば品揃え・在庫薄・店内環境を疑う
- **平均購入数**: セット販売・アタッチ販売の効き具合。小物はバンドル同梱で売れる
- **商品単価**: 季節要因が大きい（夏＝Tシャツ中心で下がる／冬＝アウターで上がる）
- **カテゴリ構成**: 売上比率 vs 販売数比率のズレ
  - 売上比率 > 販売数比率 → 単価ドライバー（アウター・デニム）
  - 販売数比率 > 売上比率 → ボリュームドライバー（半袖・Tシャツ）
- **価格帯**: ¥2,980〜3,980（数量の主戦場）と ¥5,980以上（単価源）の二層バランス

---

## 10. 認証情報の管理

**認証情報は絶対にコードに書かない。**

| 場所 | 方法 |
|---|---|
| 手元のPC | `.env` ファイル（`.gitignore` 済み） |
| GitHub Actions | GitHub Secrets |

`.env.example` が記入見本。これをコピーして `.env` を作る。

| 変数名 | 用途 | 状態 |
|---|---|---|
| `DIGITAIL_ID` / `DIGITAIL_PW` | デジテール | ✅ 必要 |
| `CASHIER_ID` / `CASHIER_PW` | cashier | ⬜ 後で |
| `AIRREGI_ID` / `AIRREGI_PW` | Airレジ | ⬜ 後で |
| `EZREGI_ID` / `EZREGI_PW` | EZレジ | ⬜ 後で |
| `GCP_SA_JSON` / `SPREADSHEET_ID` | スプレッドシート | ⬜ 後で |
| `SLACK_WEBHOOK` | Slack通知 | ⬜ 後で |
| `ANTHROPIC_API_KEY` | 示唆生成 | ⬜ 後で |

---

## 11. GitHub Actions（未実装）

```yaml
name: notime-daily
on:
  schedule:
    - cron: '0 23 * * *'   # UTC23:00 = JST 8:00
  workflow_dispatch:       # スマホのブラウザから手動実行できる

permissions:
  contents: write          # notime.db をコミットするため

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: '3.12' }
      - run: pip install -r requirements.txt
      - run: python main.py
        env:
          DIGITAIL_ID: ${{ secrets.DIGITAIL_ID }}
          DIGITAIL_PW: ${{ secrets.DIGITAIL_PW }}
          # ... 他のSecretsは §10 の表のとおり
      - run: |
          git config user.name "notime-bot"
          git config user.email "bot@notime"
          git add notime.db
          git diff --staged --quiet || git commit -m "data $(date +%F)"
          git push
```

**v1からの変更点**

- `playwright install --with-deps chromium` を**削除**（requestsで完結するため）
  → 必要になったら戻す
- コミット対象を `raw/` から **`notime.db`** に変更（§5-1の決定による）
- `workflow_dispatch` はスマホから手動実行するために必須

---

## 12. 実装の順序

| # | やること | 状態 |
|---|---|---|
| 1 | `fetch_digitail.py` を実装 | ✅ 完了 |
| 2 | `.env` を作って実行し、実データでDBに入ることを確認 | ⬜ **次はここ** |
| 3 | `fetch_cashier.py` を実装（cashier.jp を調査） | ⬜ |
| 4 | `fetch_airregi.py` / `fetch_ezregi.py` を実装 | ⬜ |
| 5 | `main.py`（司令塔・エラー通知・スモークテスト） | ⬜ |
| 6 | `push_sheets.py` | ⬜ |
| 7 | `push_slack.py` | ⬜ |
| 8 | GitHub Actions に載せる（スマホから実行可能に） | ⬜ |

### 検証方法（正解データ）

6/23時点の集計が以下と一致すればOK（リファクタ後も一致確認済み）:

| 店舗 | 売上税抜 | 購買率 | 客単価 |
|---|---|---|---|
| 山形 | ¥13,825,236 | — | ¥6,871 |
| いわき | ¥9,364,129 | 24.6% | ¥6,703 |
| 福井 | ¥7,234,488 | 29.4% | ¥7,227 |

---

## 13. ユーザーにしか答えられない未確定事項

コードを進めるうえで、いずれ確認が必要になるもの。**今すぐでなくてよい。**

1. **下北沢の開店日** — `config.py` が `2026-01-01` の仮値のまま（TODOコメントあり）。
   経過週の計算がズレる。変更禁止ファイルなので、直すならユーザーの許可が必要
2. **無人営業の実態**（§5-5）— 有人時間の来店が入店データに入っていないなら、
   購買率の解釈が変わる
3. **下北沢の入店データを使うか**（§5-2）
4. **スプレッドシートのID**と、サービスアカウントへの共有設定（§7）
5. **Slack Webhook URL**（§8）
6. **cashier / Airレジ / EZレジ のログイン情報**（§10）
